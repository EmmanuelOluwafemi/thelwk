import React from "react";
import styled from "styled-components";

const Login = () => {
  return (
    <>
      <LoginStyles>login works</LoginStyles>
    </>
  );
};

export default Login;
const LoginStyles = styled.div``;
